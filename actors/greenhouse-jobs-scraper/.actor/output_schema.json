{
  "actorOutputSchemaVersion": 1,
  "title": "Greenhouse jobs output",
  "properties": {
    "jobs": {
      "type": "string",
      "title": "Jobs",
      "description": "All Greenhouse job postings from this run as a JSON array.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
    }
  }
}
