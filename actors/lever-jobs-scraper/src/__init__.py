"""Actor shell for `lever-jobs-scraper` (SPEC v2 §3.2).

All logic lives in ``core.run``; this package only pins the provider to Lever.
``python -m src.main`` and ``python -m src`` both start it (§9.2 Dockerfile CMD).
"""
