{
  "actorOutputSchemaVersion": 1,
  "title": "Lever jobs output",
  "properties": {
    "jobs": {
      "type": "string",
      "title": "Jobs",
      "description": "All Lever job postings from this run as a JSON array.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
    }
  }
}
