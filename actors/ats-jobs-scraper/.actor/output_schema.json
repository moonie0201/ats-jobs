{
  "actorOutputSchemaVersion": 1,
  "title": "ATS jobs output",
  "properties": {
    "jobs": {
      "type": "string",
      "title": "Jobs",
      "description": "All job postings from this run as a JSON array.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
    }
  }
}
